#include<stdio.h>
void main()
{
   int a[20],n,i,j,sum,k;
   printf("enter the array size");
   scanf("%d",&n);
   printf("enter the array element ");
  // for(k=0;k<n;k++)
   //{
   for(i=0;i<n;i++)
   {
       scanf("%d %d",&a[i],&a[n]);
       
   }
   
   //for(j=n;j<n-1;j--)
   //{
     //  scanf("%d",&a[j]);
   //}
   //sum=a[i]+a[j];
  // printf("%d",sum);
   //}
}