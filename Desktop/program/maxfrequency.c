#include<stdio.h>
void main()
{
    char str[10];
    printf("enter the string");
    scanf("%s",str);
    for(i=0;i<strlen(str);i++)
    {
        max[i]=str[i];
          for(j=0;j<n;j++)
          {
              if(max==str[i])
              {
                 printf("hii");
              }
           
          }
           
    }
}